# Issue tracker

GitHub Issues in this repository.

## Workflow

- `to-tickets`, `triage`, `to-spec`, and `code-review` read from and write to GitHub Issues via the `gh` CLI.
- Issues are the source of truth for work tracking.
- PRs are not used as a request surface by default.
